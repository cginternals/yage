use crate::{
    Context,
    Texture
};

#[derive(Default)]
pub struct TextureLoader {
}

impl TextureLoader {
    pub fn load(_context: &Context, _texture: &mut Texture, _path: &str) {
        // [TODO] Implement
    }
}
